# Fully pinned direct dependencies for the complete Windows application.
# Core CLI, dashboard API, paper broker, and backtests use the standard library.

# Broker connectors
MetaTrader5==5.0.5735; platform_system == "Windows"
ccxt==4.5.64
alpaca-py==0.43.5

# Desktop application
PyQt6==6.11.0
pystray==0.19.5
Pillow==12.3.0
win10toast==0.9; platform_system == "Windows"

# Notifications and packaging
requests==2.34.2
pyinstaller==6.21.0
